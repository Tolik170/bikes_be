const USD = 'usd'
const EN = 'en'

const hashFeatures = {
  SHA_1: 'sha1',
  DIGEST: 'hex'
}

const paymentStatuses = {
  APPROVED: 'approved',
  SUCCESS: 'success'
}

orderStatuses = {
  CREATED: 'created',
  PROCESSING: 'processing',
  PAID: 'paid'
}

module.exports = {
  USD,
  EN,
  hashFeatures,
  paymentStatuses,
  orderStatuses
}
